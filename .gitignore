node_modules/
dist/
build/
.screenshots/
*.tsbuildinfo

# tsc -b emits these from vite.config.ts (tsconfig.node.json is composite).
# Derived; Vite loads the .ts.
apps/web/vite.config.js
apps/web/vite.config.d.ts
.env
.env.local
*.log
.DS_Store
apps/web/.vite/

# capture hook bookkeeping (derived state; the logs themselves are in .agent-logs/ and ARE committed)
.claude/hooks/.state/
