import { Module } from "@nestjs/common";

// Empty, wired stub. Dashboard aggregates over ClickEvent land once the brief
// defines what the dashboard shows. See docs/MAP.md.
@Module({})
export class AnalyticsModule {}
